{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['email', 'nickname']) && newData.child('nickname').isString() && newData.child('nickname').val().length >= 3"
      }
    },
    "games": {
      ".read": true,
      ".indexOn": ["status", "createdAt", "isPublic"],
      "$gameId": {
        ".validate": "newData.hasChildren(['status', 'players', 'board', 'currentPlayer', 'createdAt'])",
        ".write": "(newData.exists() && !data.exists() && auth != null && newData.child('players/black/uid').val() === auth.uid && newData.child('status').val() === 'waiting') || (data.exists() && newData.exists() && auth != null && data.child('status').val() === 'waiting' && !data.child('players').hasChild('white') && newData.child('players/white/uid').val() === auth.uid && newData.child('status').val() === 'playing') || (data.exists() && newData.exists() && auth != null && data.child('status').val() === 'playing' && ( (data.child('currentPlayer').val() === 1 && data.child('players/black/uid').val() === auth.uid) || (data.child('currentPlayer').val() === 2 && data.child('players/white/uid').val() === auth.uid) )) || (data.exists() && newData.exists() && auth != null && data.child('status').val() === 'playing' && newData.child('status').val() === 'finished' && (data.child('players/black/uid').val() === auth.uid || data.child('players/white/uid').val() === auth.uid)) || (!newData.exists() && (data.hasChild('lastUpdateAt') ? data.child('lastUpdateAt').val() : data.child('createdAt').val()) < (now - 172800000))"
      }
    }
  }
}